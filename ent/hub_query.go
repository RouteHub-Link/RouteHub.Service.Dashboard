// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	entdomain "RouteHub.Service.Dashboard/ent/domain"
	enthub "RouteHub.Service.Dashboard/ent/hub"
	"RouteHub.Service.Dashboard/ent/link"
	"RouteHub.Service.Dashboard/ent/organization"
	"RouteHub.Service.Dashboard/ent/predicate"
	"RouteHub.Service.Dashboard/ent/schema/mixin"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HubQuery is the builder for querying Hub entities.
type HubQuery struct {
	config
	ctx              *QueryContext
	order            []enthub.OrderOption
	inters           []Interceptor
	predicates       []predicate.Hub
	withDomain       *DomainQuery
	withOrganization *OrganizationQuery
	withLinks        *LinkQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HubQuery builder.
func (hq *HubQuery) Where(ps ...predicate.Hub) *HubQuery {
	hq.predicates = append(hq.predicates, ps...)
	return hq
}

// Limit the number of records to be returned by this query.
func (hq *HubQuery) Limit(limit int) *HubQuery {
	hq.ctx.Limit = &limit
	return hq
}

// Offset to start from.
func (hq *HubQuery) Offset(offset int) *HubQuery {
	hq.ctx.Offset = &offset
	return hq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hq *HubQuery) Unique(unique bool) *HubQuery {
	hq.ctx.Unique = &unique
	return hq
}

// Order specifies how the records should be ordered.
func (hq *HubQuery) Order(o ...enthub.OrderOption) *HubQuery {
	hq.order = append(hq.order, o...)
	return hq
}

// QueryDomain chains the current query on the "domain" edge.
func (hq *HubQuery) QueryDomain() *DomainQuery {
	query := (&DomainClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enthub.Table, enthub.FieldID, selector),
			sqlgraph.To(entdomain.Table, entdomain.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, enthub.DomainTable, enthub.DomainColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (hq *HubQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enthub.Table, enthub.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, enthub.OrganizationTable, enthub.OrganizationColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLinks chains the current query on the "links" edge.
func (hq *HubQuery) QueryLinks() *LinkQuery {
	query := (&LinkClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(enthub.Table, enthub.FieldID, selector),
			sqlgraph.To(link.Table, link.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, enthub.LinksTable, enthub.LinksColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Hub entity from the query.
// Returns a *NotFoundError when no Hub was found.
func (hq *HubQuery) First(ctx context.Context) (*Hub, error) {
	nodes, err := hq.Limit(1).All(setContextOp(ctx, hq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{enthub.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hq *HubQuery) FirstX(ctx context.Context) *Hub {
	node, err := hq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Hub ID from the query.
// Returns a *NotFoundError when no Hub ID was found.
func (hq *HubQuery) FirstID(ctx context.Context) (id mixin.ID, err error) {
	var ids []mixin.ID
	if ids, err = hq.Limit(1).IDs(setContextOp(ctx, hq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{enthub.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hq *HubQuery) FirstIDX(ctx context.Context) mixin.ID {
	id, err := hq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Hub entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Hub entity is found.
// Returns a *NotFoundError when no Hub entities are found.
func (hq *HubQuery) Only(ctx context.Context) (*Hub, error) {
	nodes, err := hq.Limit(2).All(setContextOp(ctx, hq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{enthub.Label}
	default:
		return nil, &NotSingularError{enthub.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hq *HubQuery) OnlyX(ctx context.Context) *Hub {
	node, err := hq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Hub ID in the query.
// Returns a *NotSingularError when more than one Hub ID is found.
// Returns a *NotFoundError when no entities are found.
func (hq *HubQuery) OnlyID(ctx context.Context) (id mixin.ID, err error) {
	var ids []mixin.ID
	if ids, err = hq.Limit(2).IDs(setContextOp(ctx, hq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{enthub.Label}
	default:
		err = &NotSingularError{enthub.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hq *HubQuery) OnlyIDX(ctx context.Context) mixin.ID {
	id, err := hq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Hubs.
func (hq *HubQuery) All(ctx context.Context) ([]*Hub, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryAll)
	if err := hq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Hub, *HubQuery]()
	return withInterceptors[[]*Hub](ctx, hq, qr, hq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hq *HubQuery) AllX(ctx context.Context) []*Hub {
	nodes, err := hq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Hub IDs.
func (hq *HubQuery) IDs(ctx context.Context) (ids []mixin.ID, err error) {
	if hq.ctx.Unique == nil && hq.path != nil {
		hq.Unique(true)
	}
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryIDs)
	if err = hq.Select(enthub.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hq *HubQuery) IDsX(ctx context.Context) []mixin.ID {
	ids, err := hq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hq *HubQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryCount)
	if err := hq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hq, querierCount[*HubQuery](), hq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hq *HubQuery) CountX(ctx context.Context) int {
	count, err := hq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hq *HubQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryExist)
	switch _, err := hq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hq *HubQuery) ExistX(ctx context.Context) bool {
	exist, err := hq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HubQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hq *HubQuery) Clone() *HubQuery {
	if hq == nil {
		return nil
	}
	return &HubQuery{
		config:           hq.config,
		ctx:              hq.ctx.Clone(),
		order:            append([]enthub.OrderOption{}, hq.order...),
		inters:           append([]Interceptor{}, hq.inters...),
		predicates:       append([]predicate.Hub{}, hq.predicates...),
		withDomain:       hq.withDomain.Clone(),
		withOrganization: hq.withOrganization.Clone(),
		withLinks:        hq.withLinks.Clone(),
		// clone intermediate query.
		sql:  hq.sql.Clone(),
		path: hq.path,
	}
}

// WithDomain tells the query-builder to eager-load the nodes that are connected to
// the "domain" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HubQuery) WithDomain(opts ...func(*DomainQuery)) *HubQuery {
	query := (&DomainClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withDomain = query
	return hq
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HubQuery) WithOrganization(opts ...func(*OrganizationQuery)) *HubQuery {
	query := (&OrganizationClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withOrganization = query
	return hq
}

// WithLinks tells the query-builder to eager-load the nodes that are connected to
// the "links" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HubQuery) WithLinks(opts ...func(*LinkQuery)) *HubQuery {
	query := (&LinkClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withLinks = query
	return hq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Hub.Query().
//		GroupBy(enthub.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hq *HubQuery) GroupBy(field string, fields ...string) *HubGroupBy {
	hq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HubGroupBy{build: hq}
	grbuild.flds = &hq.ctx.Fields
	grbuild.label = enthub.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.Hub.Query().
//		Select(enthub.FieldName).
//		Scan(ctx, &v)
func (hq *HubQuery) Select(fields ...string) *HubSelect {
	hq.ctx.Fields = append(hq.ctx.Fields, fields...)
	sbuild := &HubSelect{HubQuery: hq}
	sbuild.label = enthub.Label
	sbuild.flds, sbuild.scan = &hq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HubSelect configured with the given aggregations.
func (hq *HubQuery) Aggregate(fns ...AggregateFunc) *HubSelect {
	return hq.Select().Aggregate(fns...)
}

func (hq *HubQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hq); err != nil {
				return err
			}
		}
	}
	for _, f := range hq.ctx.Fields {
		if !enthub.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hq.path != nil {
		prev, err := hq.path(ctx)
		if err != nil {
			return err
		}
		hq.sql = prev
	}
	return nil
}

func (hq *HubQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Hub, error) {
	var (
		nodes       = []*Hub{}
		withFKs     = hq.withFKs
		_spec       = hq.querySpec()
		loadedTypes = [3]bool{
			hq.withDomain != nil,
			hq.withOrganization != nil,
			hq.withLinks != nil,
		}
	)
	if hq.withDomain != nil || hq.withOrganization != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, enthub.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Hub).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Hub{config: hq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := hq.withDomain; query != nil {
		if err := hq.loadDomain(ctx, query, nodes, nil,
			func(n *Hub, e *Domain) { n.Edges.Domain = e }); err != nil {
			return nil, err
		}
	}
	if query := hq.withOrganization; query != nil {
		if err := hq.loadOrganization(ctx, query, nodes, nil,
			func(n *Hub, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	if query := hq.withLinks; query != nil {
		if err := hq.loadLinks(ctx, query, nodes,
			func(n *Hub) { n.Edges.Links = []*Link{} },
			func(n *Hub, e *Link) { n.Edges.Links = append(n.Edges.Links, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (hq *HubQuery) loadDomain(ctx context.Context, query *DomainQuery, nodes []*Hub, init func(*Hub), assign func(*Hub, *Domain)) error {
	ids := make([]mixin.ID, 0, len(nodes))
	nodeids := make(map[mixin.ID][]*Hub)
	for i := range nodes {
		if nodes[i].domain_fk == nil {
			continue
		}
		fk := *nodes[i].domain_fk
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(entdomain.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "domain_fk" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hq *HubQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*Hub, init func(*Hub), assign func(*Hub, *Organization)) error {
	ids := make([]mixin.ID, 0, len(nodes))
	nodeids := make(map[mixin.ID][]*Hub)
	for i := range nodes {
		if nodes[i].organization_id == nil {
			continue
		}
		fk := *nodes[i].organization_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hq *HubQuery) loadLinks(ctx context.Context, query *LinkQuery, nodes []*Hub, init func(*Hub), assign func(*Hub, *Link)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[mixin.ID]*Hub)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Link(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(enthub.LinksColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.link_fk
		if fk == nil {
			return fmt.Errorf(`foreign-key "link_fk" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "link_fk" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (hq *HubQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hq.querySpec()
	_spec.Node.Columns = hq.ctx.Fields
	if len(hq.ctx.Fields) > 0 {
		_spec.Unique = hq.ctx.Unique != nil && *hq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hq.driver, _spec)
}

func (hq *HubQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(enthub.Table, enthub.Columns, sqlgraph.NewFieldSpec(enthub.FieldID, field.TypeString))
	_spec.From = hq.sql
	if unique := hq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hq.path != nil {
		_spec.Unique = true
	}
	if fields := hq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, enthub.FieldID)
		for i := range fields {
			if fields[i] != enthub.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := hq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hq *HubQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hq.driver.Dialect())
	t1 := builder.Table(enthub.Table)
	columns := hq.ctx.Fields
	if len(columns) == 0 {
		columns = enthub.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hq.sql != nil {
		selector = hq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hq.ctx.Unique != nil && *hq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hq.predicates {
		p(selector)
	}
	for _, p := range hq.order {
		p(selector)
	}
	if offset := hq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HubGroupBy is the group-by builder for Hub entities.
type HubGroupBy struct {
	selector
	build *HubQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hgb *HubGroupBy) Aggregate(fns ...AggregateFunc) *HubGroupBy {
	hgb.fns = append(hgb.fns, fns...)
	return hgb
}

// Scan applies the selector query and scans the result into the given value.
func (hgb *HubGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hgb.build.ctx, ent.OpQueryGroupBy)
	if err := hgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HubQuery, *HubGroupBy](ctx, hgb.build, hgb, hgb.build.inters, v)
}

func (hgb *HubGroupBy) sqlScan(ctx context.Context, root *HubQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hgb.fns))
	for _, fn := range hgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hgb.flds)+len(hgb.fns))
		for _, f := range *hgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HubSelect is the builder for selecting fields of Hub entities.
type HubSelect struct {
	*HubQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hs *HubSelect) Aggregate(fns ...AggregateFunc) *HubSelect {
	hs.fns = append(hs.fns, fns...)
	return hs
}

// Scan applies the selector query and scans the result into the given value.
func (hs *HubSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hs.ctx, ent.OpQuerySelect)
	if err := hs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HubQuery, *HubSelect](ctx, hs.HubQuery, hs, hs.inters, v)
}

func (hs *HubSelect) sqlScan(ctx context.Context, root *HubQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hs.fns))
	for _, fn := range hs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
